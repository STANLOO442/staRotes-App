/**import express from 'express';
import { loginPage, loginUser } from '../controller/loginController';
const router = express.Router();


/* GET LoginPage. */
// router.get('/login', loginPage);
/* POST Login */
// router.post('/login', loginUser);

// export default router;